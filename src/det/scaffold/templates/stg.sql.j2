{{ '{{' }}
    config(
        materialized="view"
    )
{{ '}}' }}

-- generated by det scaffold-dbt; edit for domain cleaning, or re-run with --force
-- to refresh the column list from the pipeline schema.
select
{% for col in columns -%}
    {{ col.expr }}{{ "," if not loop.last else "" }}
{% endfor -%}
from {{ '{{' }} det_bronze_from("{{ sql_table }}") {{ '}}' }}
