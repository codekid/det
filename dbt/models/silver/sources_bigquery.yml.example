# Superseded by dbt/models/silver/sources_bigquery.yml (checked into repo).
# Kept as a pointer for operators upgrading from the example-only layout.
#
# BigQuery sources for BigLake Iceberg bronze (architecture C) live in:
#   dbt/models/silver/sources_bigquery.yml
#
# Full recipe: docs/gcp-biglake.md
