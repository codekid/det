# Obsolete. Do not create sources_bigquery.yml alongside sources.yml.
#
# dbt 1.12 parses every sources file; duplicate source names fail even when
# config.enabled differs. BigLake + DuckDB share one file:
#   dbt/models/silver/sources.yml
#   dbt/models/ops/sources.yml
#
# Inline Jinja sets database for BigQuery and gates DuckDB meta.external_location.
# Full recipe: docs/gcp-biglake.md
