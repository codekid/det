name: "disaster_analytics"
version: "1.0.0"
config-version: 2
profile: "disaster_analytics"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  disaster_analytics:
    silver:
      # Per-model schema=silver_{provider} from scaffold (see generate_schema_name).
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
    ops:
      +tags: [ops]
      +schema: ops
      # Table (not view): Iceberg scan is cwd-sensitive; persist rows in DET_OPS_DUCKDB.
      +materialized: table
