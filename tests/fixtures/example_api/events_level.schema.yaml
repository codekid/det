# Test-only migrate target for example_api_v1_to_v2 (severity → level).
# Not a live pipeline schema — that is schemas/example_api/events/events.schema.yaml.
$schema: https://json-schema.org/draft/2020-12/schema
$title: example_api_events_level
type: object
required: [id, occurred_at, level]
properties:
  id: { type: string }
  occurred_at: { type: string }
  level: { type: string }
  state: { type: ["string", "null"] }
additionalProperties: false
