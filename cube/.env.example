# Copy to cube/.env (gitignored). Cube REST and DET MCP must share this secret.
CUBEJS_API_SECRET=dev-det-cube-secret-change-me

# Optional: Cursor/MCP → Cube REST (defaults match this Compose stack).
# DET_CUBE_BASE_URL=http://localhost:4000
# DET_CUBE_API_SECRET=dev-det-cube-secret-change-me
